{
  "version": 2,
  "buildCommands": "tsc --build",
  "installCommands": "npm install",
  "builds": [
      {
          "src": "src/index.ts",
          "use": "@vercel/node"
      }
  ],
  "routes": [
      {
          "src": "/(.*)",
          "dest": "/"
      }
  ]
}